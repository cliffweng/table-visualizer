# Production Build

Build the application for production deployment.

## Instructions

1. Run the production build:

```bash
npm run build
```

2. If the build fails, analyze the errors and fix them.

3. After a successful build, report the output size from the `dist/` folder.
